"$schema" = 'https://starship.rs/config-schema.json'
add_newline = true
format = '$all'

[character] # The name of the module we are configuring is 'character'
success_symbol = '[➜](bold green)' # The 'success_symbol' segment is being set to '➜' with the color 'bold green'
error_symbol = '[✗](bold red)'

[hostname]
ssh_only = false
style = 'fg:#ff0000'
format = '[$ssh_symbol$hostname]($style) in '
disabled = false

[localip]
ssh_only = false
disabled = false

[git_branch]
symbol = '🌱 '
truncation_length = 4
truncation_symbol = ''

# [username]
# style_user = 'blue bold'
# style_root = 'black bold'
# format = '[$user]($style) use '
# disabled = false
# show_always = true

[directory]
truncate_to_repo = true
style = 'fg:#00bfff'
[conda]
disabled = false
format = '[$symbol$environment](dimmed green) '

[memory_usage]
disabled = false
threshold = -1
symbol = '🐏'
style = 'fg:#228b22'
format = 'costs $symbol [${ram}( )]($style) '

[time]
style = 'fg:#ffc0cb'
disabled = false
format = '🕙[\[$time\]]($style) '
time_format = '%T'

[package]
format = 'pkg [🎁 $version](208 bold) '


[git_status]
conflicted = '🏳conflict'
ahead = '🏎💨'
behind = '😰'
diverged = '😵'
up_to_date = '✓u2d'
untracked = '🤷'
stashed = '📦'
modified = '📝 modi'
staged = '[++\($count\)](green)'
renamed = '👅'
deleted = '🗑'

[git_state]
format = '[\($state( $progress_current of $progress_total)\)]($style) '
cherry_pick = '[🍒 PICKING](bold red)'

[git_metrics]
added_style = 'bold blue'
format = '[+$added]($added_style)/[-$deleted]($deleted_style) '

[git_commit]
commit_hash_length = 4
tag_symbol = '🔖 '

[cmd_duration]
min_time = 500
format = 'take [$duration](bold pink)'
